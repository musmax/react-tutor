export const Clients = (props) => {
    return(
        <div className="container">
            <h1>Welcome to the the client management page</h1><br/>
            <p>Your username is: {props.username}</p>
            </div>
    );
}
